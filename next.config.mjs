/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "fakestoreapi.com",
      "dummyjson.com", "www.w3.org",
      "assets.dummyjson.com",
      "cdn.dummyjson.com",
    ],
  },
};

export default nextConfig;
